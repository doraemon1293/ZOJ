import sys
lines=[x.strip() for x in sys.stdin.readlines()]

line=0
n=int(lines[0])
line=1
while line!=len(lines):
    n=int(lines[0])
    line+=1
    arr=lines[line:line+n]
    

    
    